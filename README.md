# GitHub Pages files

Copy the `docs/` folder into the root of the `theodevelop/test-results-exporter` repository.

Then enable GitHub Pages from the `main` branch and the `/docs` folder in **Settings → Pages**.

No personal name or email address is included. The public publisher name is `theodevelop`.
